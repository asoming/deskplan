移除鼠标穿透及其快捷键；修复解除固定后的拖动。空白处和象限标题可拖动，小窗只保留新建与展开。取消最小化和关闭按钮，通过面板/托盘右键菜单退出。

新增 GitHub 正式版本检查：后台每 6 小时检查，回到日序时提示新版；设置中可以检测更新、重试和关闭自动检查。仅打开 GitHub 下载页面，不自动安装。保留现有任务和设置。

Removes mouse-through and its shortcut, fixes unlocked panel dragging, and reduces the mini rail to New task and Expand. Removes minimize/close buttons; Quit is available in the panel/tray context menu.

Adds stable GitHub release checks, deferred update notices, and manual check/retry controls in Settings. No automatic installation; existing tasks and settings are retained.

Linux deb/tar, Windows x64 installer, macOS Apple Silicon/Intel DMG/ZIP. Windows 未签名；macOS 为临时签名，未公证。Windows is unsigned; macOS is ad-hoc signed and not notarized. See attached validation reports and checksums.

- 修复任务详情「关联文件」不能拖放：支持文件和文件夹混合拖入、现有任务即时关联、新建任务草稿附件及路径去重。
- Fixed file/folder drops in task details, including mixed drops, existing tasks, draft attachments, and duplicate paths.

- 文字透明度为 0% 时取消额外淡化，提高文字对比度；透明桌面使用实心浅色文字以区分壁纸。侧栏图标仅在移入侧栏或键盘导航聚焦时显示。
- Improved text contrast and removed extra fading at 0% text transparency; sidebar icons reveal on sidebar hover or keyboard focus.

- 移除导致空心字效果的文字细边；高透明面板改用实心浅色文字，保留四象限紧急颜色。
- Removed outline-like text shadows; highly transparent panels use solid light text while keeping urgency colors.
