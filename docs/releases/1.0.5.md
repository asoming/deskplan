修复 Linux 桌面图标层拦截日序点击和拖放的问题。现在层级为桌面/图标 < 日序 < 其他应用。完全透明背景仍可点击、输入和拖入文件/文件夹，固定位置与鼠标穿透保持独立。保留任务与个性化设置。Linux 使用 X11/XWayland。

Fixes the Linux desktop icon surface intercepting clicks and drops. The order is desktop/icons < Rixu < other apps, with input and native file/folder drops preserved at full background transparency. Position locking remains separate from click-through. Existing tasks and settings are retained. Linux uses X11/XWayland.

Includes Linux deb/tar, Windows x64 installer, and macOS Apple Silicon/Intel DMG/ZIP. See the attached verification report for tests and checksums.

Windows 未签名；macOS 为临时签名，未公证。Windows is unsigned; macOS is ad-hoc signed and not notarized.
