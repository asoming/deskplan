主面板改为真正的桌面底层，点击和聚焦后也保持在普通工作窗口下方。支持文件夹、项目目录与工作空间文件的拖入、打开及重新定位，删除任务不会影响原目录。保留右上角零留白、固定位置、中英文界面与透明度设置。

The main and mini panels now stay at the desktop layer, even after focus and raise requests. Drop, open and relink folders, project directories and workspace files; removing tasks never deletes their original directories. Flush top-right docking, position locking, bilingual UI and transparency remain available.

Windows is unsigned; macOS is ad-hoc signed and not notarized. Native desktop behavior depends on the OS window manager.
