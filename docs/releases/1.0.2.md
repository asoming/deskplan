日序现在默认停靠右上角并固定位置。左侧图钉解锁后可拖动顶部空白处，再次固定；设置支持额外右侧留白。主面板和小窗不再置顶，功能入口移到左侧图标栏，面板内移除品牌图标，中英文提示同步切换。固定位置不会影响任务编辑或文件拖入。

Rixu now docks at the top right with a fixed position by default. Unlock with the sidebar pin, drag the top strip, then fix it again. An adjustable right margin leaves room for desktop files. Neither panel stays on top. A left icon sidebar replaces the top controls and panel logo, with Chinese/English labels. Task editing and file drops remain available while position is fixed.

下载对应的 Debian / Linux、Windows、macOS Apple Silicon 或 Intel 安装包。校验值与四平台测试报告随发行版提供。程序不会自动识别或整理桌面文件图标。

Windows is unsigned; macOS is ad-hoc signed and not notarized. Operating-system prompts may follow your system language. Existing task data and attachments are preserved.
