默认紧贴可用桌面右上角，移除窗口外边距和顶部文字行。右上角停靠按钮同步清除右侧留白。解锁后从左侧栏空白处拖动，固定、任务操作与双语保持可用。

The panel now sits flush with the top-right corner of the desktop work area. The outer gap and top caption row are removed. Docking resets the right margin; unlock and drag the empty sidebar area to move the panel.
